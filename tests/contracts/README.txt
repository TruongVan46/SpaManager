Frontend contract baseline changes require explicit review. Do not regenerate merely to make the test pass.

These JSON files are committed deterministic snapshots. The normal test command never overwrites them.
Baseline generation, when deliberately required, must be performed by an explicitly reviewed change and must not read .ui-audit at runtime.
